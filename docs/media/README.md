# Media

No screenshots or video ship in this tree. Media of the public build are captured separately from a
clean install with every ARGUS root at an empty directory; they show the instrument's empty and
refusal states, not results.

Claim boundary: this release ships no detector and makes no scientific claim. A rendered candidate is
not a reading. Every export packet is `never_published`.
